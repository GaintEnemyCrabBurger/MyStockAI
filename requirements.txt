# ============================================================
# MyStockAI dependencies
# Works on any Python 3.9 - 3.13 without native build steps.
# ============================================================

# ---- Web UI ----
streamlit>=1.32
plotly>=5.18

# ---- Data processing ----
# Technical indicators (MA / KDJ / RSI / MACD) are implemented
# in pure pandas inside core/indicators.py, so we intentionally
# do NOT depend on pandas-ta / TA-Lib / numba.
numpy>=1.23
pandas>=1.5

# ---- Market data source (A-shares / HK / US, free public APIs) ----
akshare>=1.12

# Notes:
# - requests / beautifulsoup4 are transitively installed by akshare.
# - No python-dotenv: this project does not use .env files.
# - No API keys / tokens required; akshare uses public endpoints.
