# ============================================================
# MyStockAI dependencies
# Target on Streamlit Cloud: Python 3.11 recommended
# ============================================================

# ---- Web UI ----
streamlit>=1.32
plotly>=5.18

# ---- Data processing & technical analysis ----
# IMPORTANT: pandas-ta (beta) relies on deprecated numpy APIs
# (np.NaN, np.bool etc.) that were removed in numpy 2.x.
# Pin numpy < 2 until pandas-ta ships a stable 0.4 release.
numpy<2
pandas>=1.5,<2.3
pandas-ta==0.3.14b0

# ---- Market data source (A-shares / HK / US, all free public APIs) ----
akshare>=1.12

# Notes:
# - requests / beautifulsoup4 are transitively installed by akshare.
# - No python-dotenv: this project does not use .env files.
# - No API keys / tokens required; akshare uses public endpoints.
